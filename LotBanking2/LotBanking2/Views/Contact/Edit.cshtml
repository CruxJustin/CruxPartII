@model LotBanking2.viewmodels.builderConnect

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>contact</legend>

        @Html.HiddenFor(model => model.contact.id)

        <div class="editor-label">
            @Html.LabelFor(model => model.contact.company)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.contact.company)
            @Html.ValidationMessageFor(model => model.contact.company)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.contact.title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.contact.title)
            @Html.ValidationMessageFor(model => model.contact.title)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.contact.name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.contact.name)
            @Html.ValidationMessageFor(model => model.contact.name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.contact.date_created)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.contact.date_created)
            @Html.ValidationMessageFor(model => model.contact.date_created)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.contact.date_modified)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.contact.date_modified)
            @Html.ValidationMessageFor(model => model.contact.date_modified)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>

    </fieldset>
    
    <!-- ----------------------------------------------------------------------------------------------------------------------------- -->
    
        <div>
        @Html.ValidationSummary(true)
       <fieldset>
        <legend>Contact Address</legend>
		@Html.Partial("editContact", Model)
        </fieldset>
        </div>

    <!-- ----------------------------------------------------------------------------------------------------------------------------- -->
    
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
